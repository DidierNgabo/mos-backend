import { PrescriptionsController } from './prescriptions.controller';

describe('PrescriptionsController', () => {
  it('should be defined', () => {
    expect(PrescriptionsController).toBeDefined();
  });
});
